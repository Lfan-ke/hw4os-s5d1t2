# casca 来源 / provenance

- 包: casca 1.0.4 —— "Native Python CLI UI library with CSS-like styling"(纯 Python TUI 框架)。
- 单 `py3-none-any`(noarch)wheel,无额外依赖;PyPI。架构无关,四架构由 base musl CPython 3.12 覆盖。
- 取材:`pip download --only-binary=:all: casca`(wheels/ 已附)。
- base 镜像:`rootfs-<arch>-python.img`。
