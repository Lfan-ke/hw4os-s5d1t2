# S3 思考题 · 内核形态

用自己的话回答下面三问（每问 2-4 句即可），把 `LABCTL_ESSAY_TODO` 那一行替换成你的作答后再交。

## Q1 unikernel 为什么能用「函数调用」替代「syscall」？这样做的代价是什么？

（提示：地址空间、特权边界、隔离 / 故障域）

## Q2 这个 RTOS 是协作式的。把它改成抢占式，最少需要补上哪一块硬件机制？协作式相比抢占式的优缺点各一条。

（提示：时钟中断 + 在 trap 里触发 `__switch`）

## Q3 exokernel 的「内核只做安全多路复用、抽象上移到 libOS」——它和微内核的差别在哪？请各举一个「内核必须管」与「应该交给 libOS」的例子。

（提示：exokernel 暴露硬件资源句柄而非抽象；微内核仍提供 IPC/调度等抽象）

---

LABCTL_ESSAY_TODO: 在此写下你的作答（替换本行）。
