# S1b 思考题 · 裸机最小标准库（在此作答）

回答以下问题（作答后删除文末哨兵行即视为完成）：

1. 为什么 Rust 的 `core` 不需要分配器，而 `alloc` 需要？`no_std` 还必须提供什么？
2. newlib 的 `_sbrk` / `_write` 为什么是把 libc 移植到新板子的关键？还有哪些桩？
3. 从 `alloc` 到 `std` 还差什么？
4. 板厂拿到刚流片的新片，软件 bring-up 的顺序大致是怎样的？为什么「最小标准库环境」是地基？

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
