# S5d 思考题

## 1. 阻塞与自旋的取舍：临界区长短、单核 vs 多核，各该选谁？为什么？

（提示：自旋抢不到时在干什么？单核上等锁者自旋，持锁者还有机会跑去释放锁吗？临界区多短才值得自旋而不是付两次上下文切换去睡再醒？`futex` 为什么“先自旋再阻塞”？）

你的回答：

## 2. 条件变量为什么必须配一把锁？为什么等待要写成 `while (!条件) wait()` 而不是 `if`？

（提示：条件变量自己保存状态吗？“条件”那段共享变量谁来保护？`wait` 若“放锁”和“睡眠”不是原子的，会不会丢掉一次 `signal` 而永远睡下去？被唤醒就一定轮到你、条件一定还成立吗？）

你的回答：

## 3. 信号量、互斥锁、条件变量三者语义有何区别？分别在什么时候用？

（提示：哪个是“独占且有归属”，哪个是“可计数且无归属”，哪个“自己不存状态、只等一个谓词”？有界缓冲为什么用 empty/full 两个信号量？顺带想想：低优先持锁、高优先等锁、中优先抢跑——这叫什么问题，怎么修？）

你的回答：

<!-- LABCTL_ESSAY_TODO: 完成作答后删除本行 -->
