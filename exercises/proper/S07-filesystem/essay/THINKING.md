# S07 文件系统 · 思考题

> 在每题下作答（替换占位行）。答案非空且命中关键字即过。
> 删除下面这行后本 essay 才算完成：
> LABCTL_ESSAY_TODO 请作答后删除本行

## 1. 块设备抽象边界：为什么 FS 只依赖 `bread/bwrite`？

（提示：FS 的所有盘上结构都表达成「第几号块的某段字节」；底层换成 RAM/virtio/真磁盘对 FS 透明。
联系 rcore easy-fs 的 `BlockDevice` trait。）

你的作答：

## 2. 为什么把「名字→inode」和「inode→数据块」分两层，而不是一个大哈希表？

（提示：硬链接、改名/移动、目录即文件。）

你的作答：

## 3. RAM 盘断电即失，要「掉电不丢」软硬件各需补什么？

（提示：硬件的持久介质与写完成语义/写屏障；软件的日志（journaling/WAL）与崩溃一致性。
与本实验「写完立即可读」的内存语义差在哪？）

你的作答：
