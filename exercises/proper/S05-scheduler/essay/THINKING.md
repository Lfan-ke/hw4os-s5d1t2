# S05 思考题（在此作答）

> 写下你的理解即可，作答后删除最后一行的占位标记。

## 1. 为什么协作式上下文切换只需保存 callee-saved（ra/sp/s0-s11），而不是全部 32 个通用寄存器？

（提示：联系 RISC-V 调用约定——caller-saved 在 `call` 处被谁溢出了？抢占式 trap 为何要存完整 TrapContext？）

你的回答：

## 2. `__switch` 执行 `ret` 后控制流落到哪里？为什么新任务“首次被调度”要预置 `ra=任务入口`、`sp=新栈顶`？

你的回答：

## 3. round-robin 与抢占式调度的区别？把本课改成抢占式最少要加什么？

（提示：协作式靠谁让出？永不让出会怎样？时钟中断在这里扮演什么角色？）

你的回答：

<!-- LABCTL_ESSAY_TODO: 完成作答后删除本行 -->
