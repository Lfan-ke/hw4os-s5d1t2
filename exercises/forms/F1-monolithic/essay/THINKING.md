# 形态 F1 · 宏内核 / 单内核 —— 思考题（essay 变体）

在每题下方写下你的理解即可（命中关键字即通过：同义词、直调 vs IPC、共享地址空间/无隔离、
一处崩全崩、xv6/Linux 等）。

## 1. 「单内核」和「宏内核」是不是一回事？宏内核到底「宏」在哪、和微内核的根本分界线是什么？

（联系：monolithic 的两个译名是否同义；所有服务同处一个特权地址空间；子系统间是
直接函数调用 vs IPC 消息。）

TODO: 在此作答。

## 2. 为什么 xv6 / Linux 这种「教学/工业主力」都选了宏内核？直接函数调用相对 IPC 到底省了什么？

（联系：一次 syscall = 几次函数调用 vs 多次上下文切换 + 跨地址空间拷贝；本课
`MONO_DISPATCH ipc_msgs=0 hops=3` 的含义。）

TODO: 在此作答。

## 3. 「一处崩溃全崩」具体怎么发生？本课 `off=5` 的越界写对应真实内核里的什么 bug？为什么宏内核挡不住、而微内核能？Linux 内核模块（LKM）让这个问题更轻还是更重？

（联系：同一地址空间=无隔离边界；驱动缓冲区溢出/野指针/DMA 越界踩坏调度器；
微内核把驱动放用户态靠 MMU 隔离；eBPF / 用户态驱动 / Rust for Linux 的补救思路。）

TODO: 在此作答。

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
