# 19-isa-emulator 思考题（essay 变体）

在每题下方写下你的理解即可（命中关键字即通过：取指/译码/执行、ISA 契约、
可信 oracle/第一处分歧、解释器/二进制翻译/真硬件/跨架构等）。

## 1. 用一句话概括 CPU 的本质，并解释「取指→译码→执行」三步各做什么。

（fetch 读机器码 / decode 拆字段+符号扩展 / execute 改寄存器·内存·pc。）

TODO: 在此作答。

## 2. 为什么说 ISA 是「软硬件之间的契约」？指令编码里的 `opcode/funct/imm` 字段体现了什么？

（同一份 ISA 让程序在 NEMU/QEMU/真芯片上行为一致；字段是契约的二进制条文。）

TODO: 在此作答。

## 3. DiffTest（差分对拍）为什么强？它如何把「最终错了」定位到「从哪条指令开始错」？

（可信 oracle + 每步对拍抓第一处分歧 = 第一条出错的指令。）

TODO: 在此作答。

## 4. 解释器 vs 二进制翻译（对照 `proper/S18-tcg`）vs 真硬件 vs 跨架构模拟器（QEMU），四者关系？

（解释器每条现场译码执行；二进制翻译翻一次缓存复用求速度；真硬件物理实现 ISA；
跨架构模拟器在宿主 ISA 上跑客户 ISA。）

TODO: 在此作答。

## 5. 引申：这颗玩具 CPU 还能怎么长大？（加指令/CSR/异常、接真 Spike/QEMU 作参考、加 watchpoint）

TODO: 在此作答。

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
