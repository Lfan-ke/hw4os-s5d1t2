# 14 · 思考题（essay · 特权级 = 连了几根线）

> 在每个标题下写下你的理解（中文/英文皆可）。言之有物、命中关键概念即通过。
> 完成后，把文末的占位标志改成完成标志（见文末说明）。

## 1. 放权自由、提权受控

提示要点：`DROP` 只会把权力变小（安全），所以能直接写 `cur_priv` 触发器；提权若也能直接写，
C 态一句 `cur_priv=A` 就拿到最高权、保护体系崩塌——所以唯一的门是 `ECALL`（陷入到固定入口、
落地点不由用户选）。联系 `sret`/`mret` 为什么只能"恢复"`SPP`/`MPP` 而不能任意指定目标特权。

（在此作答……）

## 2. 特权级数量 ↔ 保存前态位宽 ↔ 陷入复杂度

提示要点：`saved_priv` 位宽 ≥ `ceil(log2(特权级数))`（两态 1 位、三态 2 位）；为何真实
`sstatus.SPP` 1 位够、本课三态要 2 位；多一级带来的译码/委托（`medeleg`）/嵌套陷入栈成本；
H 扩展加一层虚拟化特权（VS/VU）时，硬件多付出的整套 CSR 复制 + 两阶段地址翻译。

（在此作答……）

## 3. 能力 = 特权够 且 使能位亮（两道与门）

提示要点：举真实例子（`satp` 开 MMU / `mstatus.SUM` / PMP `L` 锁定位）；为什么"特权够 AND
使能亮"比只看特权级更安全/更灵活——正交解耦、默认安全/最小权限、可锁定（连 M 态都改不了）。

（在此作答……）

---

（完成说明：三题都作答后，把下面这行改成 `本思考题已作答完毕`，并在其后追加
meta.toml 中 `[judge].expect` 里那些全大写完成标志词，判题以此为闸门。）
本思考题尚未作答完毕：TODO

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
