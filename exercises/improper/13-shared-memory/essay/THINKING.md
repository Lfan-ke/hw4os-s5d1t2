# 思考题 · 13 共享内存（在此作答）

> 判题：本文件需命中三个「自检串」——共享环的两个（RING 段、MMIO_SHM 段）与总通过串。
> 它们的写法见 README 判题口径（形如 `XXX` + `_PASS`）。请在认真作答后，把这三个串
> 原样补到文末「完成自检」一行（表示你已读懂全部环节）。
> 现在文末那一行**留空**，所以本题尚未通过——答完后补全即可。

## 1. 共享内存（零拷贝）vs 消息传递（拷贝）

各自的同步复杂度、失败模式与适用场景？各举一例说明谁更划算。

（在此作答）

## 2. 「OS 所在的 CPU 也是一台物理设备」

论证进程↔进程共享与设备↔OS（MMIO）共享是同一模型；两种场景里的「协调方/仲裁者」分别是谁？

（在此作答）

## 3. cache 一致性 / DMA 一致性 / 直接映射区

两个 CPU 核、或 CPU 与 DMA 设备共享同一物理内存时会出什么问题？
为什么 SMP 多核的「拍卖行」槽位要放在直接映射区，而不是各自的虚拟地址空间里？

（在此作答）

---

完成自检（把三个自检串补到本行冒号后）：

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
