# 03b · 动态链接加载器 思考题（在每题下作答即可）

> 判据：答案非空且命中关键字（如 `共享` / `一份` / `GOT` / `私有` / `符号解析` /
> `基址` / `重定位` / `首次调用`）即过 `ESSAY_PASS`。用自己的话写清楚即可。

## 1. 静态 vs 动态：空间与「修 bug」的代价

为什么静态链接的程序「改一个 libc bug 要逐个重新编译」，而动态链接只需「换一个 `.so`」？
从本实验的 `static_total_space`（各塞一份）vs `dyn_total_space`（一份共享 + 各自 GOT）解释空间差。

<!-- TODO: 在此作答。提示：各塞一份 / 一份共享 / n 倍 / 换 .so 全体生效 / 逐个重编。 -->

## 2. 为什么 GOT 每进程私有，而库的代码可以多进程共享？

本实验里每个动态程序有自己的 GOT（解析后各自回填）。真实世界里多个进程共用同一份 libc
代码页，却各有一份 GOT——为什么代码能共享、GOT 不能？

<!-- TODO: 在此作答。提示：只读代码页 / 位置无关 PIC / ASLR 基址不同 / GOT 在可写数据段 / COW 私有。 -->

## 3. lazy 绑定 vs now 绑定的取舍

本实验 `now_bind` 加载时解析全部符号、`lazy_bind` 只解析被调用的。各自的好处与代价是什么？
现代发行版为什么默认 now binding + 把 GOT 标只读（RELRO）？

<!-- TODO: 在此作答。提示：首次调用才解析 / 启动快慢 / 未用符号省解析 / RELRO 只读 / 防 GOT 覆写安全。 -->

## 4. 我们的模型离真实 `ld.so` 还差什么？

本实验的 `lookup` 按【符号名 key】查表，对应真实 `ld.so` 的什么步骤（提示：`find_sym`）？
模型还简化掉了哪些真实机制（举两个，如重定位类型 / ASLR 随机基址 / page cache 按 inode 共享）？
——补齐这些就成了正经实验 `S09b-linking` 里用真 `gcc`/`readelf` 跑的版本。

<!-- TODO: 在此作答。提示：符号解析 find_sym / R_*_JUMP_SLOT 重定位 / 库基址=ASLR随机 / page cache 共享只读代码页。 -->

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
