# 12 地址空间 · 思考题（essay 变体）

> 在本文件作答即可。判题只看「非空 + 命中关键词」，但请认真把心智模型写清楚。
> 关键词锚点（务必在行文里自然出现）：**反置 / inverted、hash、稀疏、直接映射 / identity、SV39**。

## 思考题 1 · 反置页表 vs 多级页表

当「虚拟地址巨大而稀疏、物理内存很小」时：

- TODO: 多级页表（如 SV39 三级）的**内存占用**与**查找成本**各如何？为什么稀疏访问下它「只建用到的中间表」？
- TODO: 反置页表（inverted page table）为什么用 `hash(pid, vpn)` 索引？它的表大小由谁决定（物理帧数 vs 虚拟空间大小）？
- TODO: 反置页表牺牲了什么（共享映射困难、碰撞链 collision chain、查找最坏情况）？真实系统里谁用过（PowerPC / IA-64 / UltraSPARC）？

（在此作答……）

## 思考题 2 · 为什么公共交换区要放「直接映射段」（va == pa）

- TODO: 跨地址空间 / 跨核如何就「同一块物理内存」达成共识？为什么用 identity（`pa==va`）而非各自的虚拟地址？
- TODO: 联系 trampoline、E3 的 SMP 槽位——为什么多槽要放在直接映射区交换信息？

（在此作答……）

## 思考题 3 · 「偷梁换柱」省了什么

- TODO: E1 若真按 `0x100000000000` 物理铺开需要多少帧？软件 MMU 只映用到的 vpn 省下了什么？
- TODO: 为什么在 NoMMU 机器上仍能靠软件垫片「虚拟」出一个 MMU？
- TODO（选答）: SV39 为何 39 位 / 三级 / 每级 9 位？换 SV48 要改哪里？

（在此作答……）

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
