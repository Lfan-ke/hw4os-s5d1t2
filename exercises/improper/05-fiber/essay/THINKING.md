# 05-fiber 思考题（在此作答）

> 作答要求：每题给出清楚的解释，命中关键概念（callee-saved / 用户态 / TLS / yield）。
> 完成全部小题并补齐文末「自检清单」后，essay 变体方可通过。

## 1. 为什么协作式切换只需保存 callee-saved 寄存器？

<!-- TODO: 联系 RISC-V 调用约定，说明 caller-saved 为何已被编译器在每个 call 处替你溢出，
     因此 switch_ctx 只需快照 ra/sp/s0–s11。 -->

## 2. 换成纤程省了哪些内核开销？什么时候不划算？

<!-- TODO: 列出省掉的内核态开销（特权级穿越 / TLS 重载 / 内核栈切换 / 调度器陷入），
     再说明纤程不划算的场景（长时间不让出会饿死同伴；阻塞式系统调用卡住载体线程）。 -->

## 3. 为什么「永不让出」的异步任务等价于顺序批处理？

<!-- TODO: 说明单线程无抢占、唯一切换点是 yield；要交错最少需引入一个让出点；
     并对比下一课「无栈协程 poll」在让出方式上的异同（换栈 vs 存状态机字段）。 -->

## 测量（选做）

<!-- TODO: 选做——用 rdcycle 量一次 switch_ctx 与一次 getpid 的周期数对比，填入数字。 -->

---

## 自检清单（完成后把 [ ] 改为 [x]，并补全对应结论关键词）

- [ ] 阶段①手写切换通过
- [ ] 阶段②运行时调度通过
- [ ] 阶段③顺序批处理通过
- [ ] 阶段④交错执行通过
- [ ] 阶段⑤类库设施通过
- [ ] 关键词命中：callee-saved / 用户态 / TLS / yield

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
