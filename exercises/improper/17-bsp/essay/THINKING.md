# 17 · 思考题（essay · BSP 与设备树）

> 在每个标题下写下你的理解（中文/英文皆可）。言之有物、命中关键概念即通过。
> 完成后，把文末最后一行的占位标志改成完成标志（见说明）。

## 1. bootloader + 设备树都不变、只升级 OS，为什么仍能开机？

提示要点：把"设备树"当作 firmware↔OS 的**稳定 ABI** 来论证（firmware 把 DTB 放进 `a1`，
内核现场解析、不写死地址）；新内核读老 DT 靠"缺失属性给默认、未知属性跳过"（本课 `parse_dt_v2`/`UPGRADE_PASS`）。
反过来：若新 OS 想用一个 DT 里压根没描述的新设备，该谁动手——重出 DT/重刷 bootloader，
还是 OS 自己去探测？各自代价是什么（能否改固件 / 可移植性 / 误探）？

（在此作答……）

## 2. 同一个 kernel 二进制凭什么跑遍 board A/B？

提示要点：`kmain` 只认 `cfg.uart_base`，板间差异收敛进一个 `BoardConfig`；
把"硬编码地址"换成"读 DT"省了什么（N 块板不再要 N 份内核）、又引入了什么成本
（解析开销、对 DT 正确性的强依赖）。对照 rcore 写死的 `UART=0x1000_0000`、`CLINT=0x0200_4000`，
若要把它们"BSP 化 / DT 化"，你需要动哪几处、新增哪个解析步骤？

（在此作答……）

## 3. 驱动靠 `compatible` 字符串去认硬件——灵活在哪、代价在哪？

提示要点：相比 01-hw-vlan 里硬连死的 MMIO 地址，"字符串解耦"灵活在
"加设备=DT 加节点 + 注册一个驱动、框架不改"，代价在字符串约定易拼错/查不出、匹配开销、裁决规则。
再放眼真实世界：x86 的 **ACPI**、ARM/RISC-V 的 **Device Tree**、PCIe 的**即插即用枚举**，
各自把"板级信息"存在了哪里、由谁产生、由谁消费？

（在此作答……）

---

（完成说明：三题都作答后，把下面这行改成 `本思考题已作答完毕`，并在其后追加
meta.toml 中 `[judge].expect` 里那个全大写完成标志词，判题以此为闸门。）
本思考题尚未作答完毕：TODO

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
