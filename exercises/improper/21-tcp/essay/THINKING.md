# 21-tcp 思考题（essay 变体）

在每题下方写下你的理解即可（命中关键字即通过：三次握手/防旧连接、TIME_WAIT/2MSL、
滑动窗口/流量控制、拥塞控制/cwnd、TCP/UDP 取舍、连接=状态+序号 等）。

## 1. 为什么握手是三次而不是两次？

（提示：连接的本质是双方对「初始序号」达成一致；第三次握手让发起方亲自确认，
杜绝「旧连接的延迟 SYN」凭空建连。）

TODO: 在此作答。

## 2. 为什么主动关闭方要停在 TIME_WAIT 等 2MSL，而不是立刻 CLOSED？

（提示：保证最后那个 ACK 能可靠送达——对方丢了 ACK 会重传 FIN；并让本连接的旧报文
在网络中彻底消散，避免窜进复用同一四元组的新连接。）

TODO: 在此作答。

## 3. 粗讲滑动窗口与拥塞控制：它们各自解决什么问题？

（提示：滑动窗口=流量控制，别淹了接收方（看对方通告 window）；拥塞控制=别压垮网络
（cwnd + 慢启动/拥塞避免/AIMD）。实际能发 = min(通告窗口, cwnd)。）

TODO: 在此作答。

## 4. TCP vs UDP：各自的取舍？什么场景该用谁？

（提示：TCP 面向连接、可靠有序、有流控/拥塞，代价是时延与复杂度；UDP 无连接、低时延、
保留消息边界。先问「丢一个包能不能忍」。）

TODO: 在此作答。

## 5. 对照 proper/S11（我们只做了 UDP）：升级成 TCP 至少要补哪些机制？

（提示：连接状态机+连接表、握手/挥手、seq/ack 与可靠重传(RTO)、接收端重组、
滑动窗口、拥塞控制……核心都在「为每条连接持续维护状态」。）

TODO: 在此作答。

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
