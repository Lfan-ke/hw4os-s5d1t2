# 06 思考题 · 无栈协程（在此作答）

> essay 变体：判题读本文件，命中关键字 + 完成自检锚点即过。
> 把下面三题用自己的话答完整，并补上文末「自检锚点」一行（对账 sw-rust / sw-c 的运行结果）。

## 1. 为什么无栈协程也叫「绿色线程」？省掉了哪些开销？

TODO: 它在用户态调度、不陷入内核，省掉了哪些开销（特权级切换 / 内核栈 / TLB·缓存抖动 /
调度器锁 / TLS）？相比有栈协程，「无栈」还额外省掉了什么（每任务一根独立栈）？

（在此作答……）

## 2. 无栈 vs 有栈：各自的「不能」与「自然」

TODO: 无栈为什么不能从任意深的调用栈中间 yield（联系函数染色 / red-blue function）？
有栈为什么内存更大、却编程模型更自然？各举一个更合适的场景。

（在此作答……）

## 3. 「无栈协程 = 状态机 = 硬件 FSM」是同一台机器吗？

TODO: 把 06.1 手写 enum 状态机、06.4 编译器生成的 Future、硬件 FSM 三者并排，它们是同一台机器吗？
这与第 01 课「软件能做的硬件也能做」如何呼应？若芯片「流片成一个协程」，每个时钟沿就是一次 poll，
那 Pending / Ready 对应硬件的什么信号？

（在此作答……）

## 自检锚点（self-check）

TODO: 作答完成后，在此行如实列出你在软件变体中跑通的各 `*_PASS` 判题串
（即 06.1 / 06.2 / 06.3 各段以及全过标记，照抄 sw-rust 或 sw-c 的实际输出）。

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
