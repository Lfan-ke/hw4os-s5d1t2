# 10 · 思考题（essay · swap）

> 在每个标题下写下你的理解（中文/英文皆可）。言之有物、命中关键概念即通过。
> 完成后，把最后一行的占位标志改成完成标志（见文末说明）。

## 1. thrashing：为什么是"断崖"，不是"斜坡"

提示要点：工作集略大于帧数 vs 远大于帧数时命中率的差别；victim 恰好是马上要用的页导致的"换出—立刻换入"循环；联系 Linux `swappiness`、OOM killer；以及"内存只是慢设备缓存"这一视角。

（在此作答……）

## 2. 持久为什么必须 sync/write-back，且回写有顺序要求

提示要点：写只置 dirty、数据还在易失的快设备；`sync_all`/`fsync`/`msync` 才落盘；
"指向数据的元数据必须在数据落盘之后才生效"（ordered write-back / journaling / WAL）；`sync_all` 前掉电会怎样。

（在此作答……）

## 3. 三场景各自换取了什么、放弃了什么

提示要点：场景一/二/三分别对应 DRAM+SSD / Linux swap / 分层内存·CXL·JBOD；
为什么不"全用快的"（容量/成本/持久），也不"全用慢的"（速度）；
软件 `translate()` 垫片 vs 硬件地址译码器各省/费了什么。

（在此作答……）

---

（完成说明：三题都作答后，把下面这行改成 `本思考题已作答完毕`，并在其后追加
meta.toml 中 `[judge].expect` 里那个全大写完成标志词，判题以此为闸门。）
本思考题尚未作答完毕：TODO

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
