# 11 堆与栈 · 思考题（在每问下方作答）

> 作答非空且命中关键字即过；并把你已经亲手点亮的里程碑抄到文末「里程碑自查」。
> 提示关键字：`向下生长`、`global_allocator`、`注册`、`guard`、`比较器`、`地址空间`、`隔离`。

## 1. 为什么栈「向下生长」、堆「向上生长」是约定而非物理必然？

（把方向对调、或把 SP「先写后减」改「先减后写」，会改变什么、不会改变什么？联系真实 layout 与调用 ABI。）

> 在此作答……

## 2. rust 一行 `#[global_allocator]` vs C 手动注册，编译器替你做了哪件事？

（你在 C 里是怎么「手动注册」的？这件「注册」的本质是什么？）

> 在此作答……

## 3. 比较器检测碰撞 与 硬件 MMU/MPU guard page 的关系？

（为什么两块独立设备天然互不侵犯？对「隔离 vs 共享同一地址空间」有什么启示？）

> 在此作答……

---

## 里程碑自查

> 作答完成后，把你在 `labctl run` 里验证过的里程碑名抄到这里（例如 `STACK_PASS` …）。
>
> 我已点亮：（待填）

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
