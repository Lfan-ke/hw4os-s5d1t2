# 15 · 思考题（essay · 启动握手）

> 在每个标题下写下你的理解（中文/英文皆可）。言之有物、命中关键概念即通过。
> 完成后，把文末占位标志改成完成标志（见说明）。
> 注意：正文里请勿出现 meta.toml `[judge].forbid` 里列的那几个全大写禁止串
> （失败串、异常串、抢跑故障串等），否则会被判不通过。

## 15.1 观察“坏掉的启动”：缺了哪一位？

跑给定的 `broken_boot`（软件 `stage_lock` / 硬件 LOCK 用例）：未做任何握手就直接读
`DATA`，读回 `0x0BADB007`，`STATUS` 里 LOCKED/BADCLK/NOTEN 三位都亮着。

提示要点：分别说清这四位各对应“缺了哪一步握手”——未写 magic 解锁（LOCKED）、
`CLKDIV=0`（BADCLK）、未置 `CTRL.EN`（NOTEN）、PLL 还没锁定（!READY）。
为什么这种“半睡半醒”的设备读回的是 `0xBADB007` 这种胡话而不是 0？

（在此作答……）

## 1. SBI/BootROM 替内核“置了哪些位”？为什么不放进内核自己做？

提示要点：rcore 里 SBI（RustSBI/OpenSBI）、xv6 的 `entry.S`→`_start`，在内核 `rust_main`
之前替你做了什么（搬运、设置栈、委派中断、时钟/串口最基础初始化、把 `a1=dtb` 指针递进来）；
为什么“先于主程序”这件事要靠**固件分层**和**链接顺序**（`ENTRY`/`.init_array`/crt0），
而不是内核自己第一条指令就做。

（在此作答……）

## 2. 为什么真实硬件常要“magic 解锁 + lock 寄存器”这种看似多余的握手？

提示要点：给一个“没有锁就会被误写/毛刺误触发而出事”的场景（看门狗解锁序列、
Flash/选项字节写保护、JTAG 解锁、时钟配置寄存器的 key）。magic 值 + 二次确认
如何挡住“总线毛刺/跑飞的指针偶然写中关键寄存器”。

（在此作答……）

## 3. `CLKDIV=0` 为何“执行错乱”？轮询 READY 的取舍？纯二进制如何保证前置程序先跑？

提示要点：`CLKDIV=0` 对应真实硬件 PLL 未锁定 / 时钟没起 → 取指乱掉；
忙等轮询 `READY` 体现“忙等 vs 中断/超时”的取舍（简单但浪费、无超时会卡死）；
纯二进制（无 ELF 运行时、没有 `.init_array` 帮你遍历）时，如何靠 `linker.ld` 的
`ENTRY` 与段顺序、把 boot 段 `KEEP` 排到最前，保证那段前置程序先跑。

（在此作答……）

---

（完成说明：三题都作答后，把下面这行改成 `本思考题已作答完毕`，并在其后追加
meta.toml 中 `[judge].expect` 里那几个全大写完成标志词，判题以此为闸门。）
本思考题尚未作答完毕：TODO

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
