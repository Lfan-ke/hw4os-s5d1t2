# 仅供人用：make sim / wave / lint（判题走 labctl，不经此）。
TOP := tb_boot
DUT := boot_gate
LABROOT := $(shell git rev-parse --show-toplevel 2>/dev/null || echo $(CURDIR)/../../../../..)
include $(LABROOT)/common/mk/verilog.mk
