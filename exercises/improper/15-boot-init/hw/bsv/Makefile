# 仅供人用：make sim / wave（判题走 labctl，不经此）。
TOP   := mkTbBoot
ENTRY := BootGate.bsv
LABROOT := $(shell git rev-parse --show-toplevel 2>/dev/null || echo $(CURDIR)/../../../../..)
include $(LABROOT)/common/mk/bsv.mk
