# 18-5 思考题 · 从 MCU 裸中断到 MPU 受控 syscall（作答区）

> 母题：**中断 = 硬件驱动的间接跳转；syscall = 被 ABI 驯化、过特权级的受控陷入。**
> 在每一节下作答；答完把文末「自评清单」逐项确认（在横线上写出对应判据词）。
> 判题按 essay 口径：答案需命中关键概念，并补全自评清单里的判据词。

## 1. 为什么 OS 不让用户态直接跳到内核函数地址？

- 隔离：用户态若能跳进任意内核地址，会绕过什么？
  - TODO: 作答……
- ABI 稳定：把「地址」换成「调用号」对前向兼容意味着什么？
  - TODO: 作答……
- 安检门：`ecall` 把所有跨界请求收口到单一入口，好处是？
  - TODO: 作答……

## 2. 向量化 vs 非向量化分发：多连几根线，省掉哪段软件？

- direct 模式下软件进门后还得自己做什么（读 cause + 分派）？
  - TODO: 作答……
- vectored 模式硬件多了什么、省了软件哪段开销？
  - TODO: 作答……
- 接回 `01-hw-vlan` 的软硬成本核算：各举一个该选 vectored、该选 direct 的场景。
  - TODO: 作答……

## 3. RISC-V `ecall`+寄存器约定 vs CISC 专用 `syscall`/`int 0x80`

- 把「分发/取号」的负担放硬件还是软件，各自代价是什么？
  - TODO: 作答……

## 4. 固定 `a7`=号、`a0..a5`=参、`a0`=返 的 ABI 意义

- libc 在这条约定里扮演什么角色？
  - TODO: 作答……
- 如果每个内核版本都改寄存器约定，会对已编译的二进制造成什么后果？
  - TODO: 作答……

---

## 自评清单（答完逐项确认，在横线上写出对应判据词）

- [ ] S1 向量分发：direct 恒为 base、vectored = base+4*cause、accept 跟随 trap_req：______ / ______ / ______ / ______
- [ ] S2 Trap 上下文：整存整取、`sepc+=4`、其余 31 个寄存器逐位不变：______
- [ ] S3 Syscall ABI：write/exit/getpid 正确分发、未知号 -ENOSYS：______
- [ ] S4 真实陷入：`ecall`/`syscall` 往返打出 HELLO_SYSCALL、返回值正确：______
- [ ] 能讲清「MCU 裸中断查表跳转 → MPU 受控 syscall 过安检」这条演化线：______

<!-- LABCTL_ESSAY_TODO：在上方写下你的作答，然后删除本行即视为完成 -->
