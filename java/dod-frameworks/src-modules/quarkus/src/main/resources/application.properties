# Quarkus JVM-mode DoD config. Fixed loopback bind (starry IPv6 stub / IPv4-only net stack).
quarkus.http.host=127.0.0.1
quarkus.http.port=18083
# Disable the dev-time banner/extra threads we do not need for a one-shot self-test.
quarkus.banner.enabled=false
quarkus.log.level=WARN
